function phoenixPackage() {
    return '';
};

$(function() {

    // Custom Controllers
    /*
	var controller = [{
		file: 'px/custom/controller/exemploCrtl.js'
	}];
	*/

    // loop em controller
    /*
	$.each(controller, function(i, item) {
		$("<script/>").attr('src', phoenixPackage() + item.file).appendTo($('head'));
	});
	*/
});
